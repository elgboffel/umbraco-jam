version: 2.1
orbs:
  win: circleci/windows@2.2.0
jobs:
  build:
    executor:
      name: win/default
      shell: powershell.exe
    steps:
      - checkout
      - run:
          working_directory: ~/backend/src/Project/UmbracoJAM/code
          command: msbuild /t:Build